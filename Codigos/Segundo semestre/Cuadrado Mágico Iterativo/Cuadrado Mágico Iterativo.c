#include <stdio.h>
#include <stdlib.h>

#define N 3

void mostrarTablero(int tablero[N][N]) {

    for (int i = 0; i < N; ++i) {
        for (int j = 0; j < N; ++j) {

        }
    }
}

void main() {
    system("pause");
}